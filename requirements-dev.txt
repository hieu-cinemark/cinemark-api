# Test-only dependencies - not installed in the Docker image (see
# Dockerfile, which only ever installs requirements.txt). Install locally
# with: pip install -r requirements-dev.txt -r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
